# 3D Reconstruction Pipeline - Python Dependencies
# Core requirements for COLMAP + gsplat + Hetzner S3 integration

# Core scientific computing and ML
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
scikit-image>=0.20.0

# PyTorch with CUDA support (install separately with CUDA index)
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# Computer vision and image processing
opencv-python>=4.7.0
opencv-contrib-python>=4.7.0
pillow>=9.5.0
imageio>=2.28.0
matplotlib>=3.7.0
plotly>=5.14.0

# 3D processing and point clouds
trimesh>=3.21.0
open3d>=0.17.0
plyfile>=0.7.4

# Data handling and serialization
pandas>=2.0.0
h5py>=3.8.0
pyyaml>=6.0
toml>=0.10.2

# Progress bars and CLI utilities
tqdm>=4.65.0
click>=8.1.0

# Network and HTTP
requests>=2.28.0
urllib3>=1.26.0

# S3 storage integration
boto3>=1.34.0
botocore>=1.34.0

# Build tools for gsplat
ninja>=1.11.0
packaging>=23.0

# Development and testing (optional)
pytest>=7.3.0
pytest-cov>=4.0.0

# Note: gsplat is installed from git in deployment script:
# pip install git+https://github.com/nerfstudio-project/gsplat.git

# Note: PyTorch should be installed with CUDA support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
